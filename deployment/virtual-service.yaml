apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: helloworld1
spec:
  hosts:
  - helloworld
  http:
  - route:
    - destination:
        host: helloworld
        subset: F001