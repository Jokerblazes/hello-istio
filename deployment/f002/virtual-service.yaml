apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: helloworld-f002
spec:
  hosts:
    - helloworld
  http:
  - match:
    - headers:
        end-user:
          exact: dev1
    route:
    - destination:
        host: helloworld
        subset: f002